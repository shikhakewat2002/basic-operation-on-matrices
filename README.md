# basic-operation-on-matrices
i have performed operations like addition, subtraction, matrix multiplication, element-wise multiplication, scaler multiplication on matrices and also displayed specific rows and columns of the matrix , it also takes the input from the user only.
